#include<stdio.h>
int main()
{
	int a,b,c;
	char ch;
	do
	{
		printf("Enter 2 nos ");
		scanf("%d %d",&a,&b);
		c=a+b;
		printf("Sum = %d\n",c);
		
		printf("Like to cont ... (y/n) ");
		fflush(stdin);
		scanf("%c",&ch);
	}while(ch=='y' || ch=='Y');
	return 0;
}
